# Backend Environment Variables

# PORT: The port on which the backend server will listen.
PORT=3000

# ALLOWED_ORIGIN: Comma-separated list of origins allowed to access the backend.
# Use "*" for allowing all origins (less secure, good for development).
ALLOWED_ORIGIN=*

# STORAGE_MODE: Determines where the application stores data.
# Options: "RAM" (in-memory, ephemeral) or "DB" (persistent database).
STORAGE_MODE=RAM

# MONGO_URI: The connection string for MongoDB when STORAGE_MODE is "DB".
# Example for a local MongoDB instance: mongodb://localhost:27017/aether_blind_relay
MONGO_URI=mongodb://mongo:27017/aether_blind_relay

# MESSAGE_TTL: Time-to-live for messages in seconds before they are purged.
# Example: 86400 seconds = 24 hours.
MESSAGE_TTL=86400